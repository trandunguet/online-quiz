## Online Quiz - group 4 project.
# Requirement
- nodejs
- mongodb
- nodemon

# Setup
- `$ npm install`
- `$ echo 'export NODE_ENV=development' >> ~/.bashrc`

# Run

- `$ sudo service mongod start`
- `$ npm start`
