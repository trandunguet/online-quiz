extends layout

block content
  .jumbotron.jumbotron-fluid
    .container
      h1 My quizzes
      a.btn.btn-primary(href='/new-quiz') New quiz

  .container
    ul.list-group
      each quiz in quizzes
        li.list-group-item !{quiz.name}
